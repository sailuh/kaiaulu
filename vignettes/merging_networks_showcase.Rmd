---
title: "Merging Networks via Identity"
author: "Carlos Paradis"
output: html_document
---

```{r}
rm(list = ls())
seed <- 1
set.seed(seed)
```

```{r warning=FALSE,message=FALSE}
require(kaiaulu)
require(data.table)
require(stringi)
```

```{r}
perceval_path <- "/Users/cvp/Desktop/kaiaulu/kaiaulu_env/bin/perceval"

git_repo_path <- "/Users/cvp/Desktop/kaiaulu_project_input/git_repos/APR/.git"
project_git <- parse_gitlog(perceval_path,git_repo_path)

mbox_path <- "/Users/cvp/Desktop/kaiaulu_project_input/mboxes/apr-dev.mbox"
project_mbox <- parse_mbox(perceval_path,mbox_path)
```


```{r}
all_name_emails <- c(unique(project_git$data.Author),unique(project_mbox$data.From))
name_mapping <- data.table(raw_name=all_name_emails,id=assign_exact_identity(all_name_emails))
head(name_mapping[,.(authors=stri_c(raw_name,collapse=",")),by="id"])
```

